apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kafka
  namespace: kafka
spec:
  template:
    spec:
      containers:
      - name: broker
        resources:
          requests:
            cpu: 100m
            memory: 1024Mi
          limits:
            memory: 2048Mi
  volumeClaimTemplates: []
